library project Graphml_Writer_Lib is

   for Languages use ("Ada");
   for Library_Name use "graphml_writer_lib";
   for Source_Dirs use ("src");
   for Object_Dir use "obj";
   for Library_Dir use "lib";
   for Library_Kind use "Static";

   package Compiler is
      for Switches ("ada") use ("-gnatf", "-gnatwa");
   end Compiler;

end Graphml_Writer_Lib;

